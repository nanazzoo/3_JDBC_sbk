<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
<comment>member-query.xml file</comment>


	<!-- selectMyTank -->
	<entry key = "selectMyTank">
		SELECT TANK_NO 번호, TANK_NAME 이름, FRESH_SALT "해수/담수", TANK_SIZE "어항 크기" , TANK_LIGHT "조명", TANK_FILTER "여과 장치" 
		, TANK_ADDICTIVE 첨가제 , TANK_SUBSTRATE 바닥재, (SELECT COUNT(*) FROM AR_LIVESTOCK L WHERE L.TANK_NO = T.TANK_NO) 생물수
		FROM AR_TANK T
		WHERE MEMBER_NO = ? AND TANK_NO = ?
	</entry>
	
	<!-- 어항 정보 수정 -->
	<entry key = "updateTank">
		UPDATE AR_TANK SET
		? = ?
		WHERE TANK_NO = ?
	</entry>
	

</properties>
